spring:
  application:
    name: springrestdemo
  cloud:
    consul:
      host: localhost
      port: 8500
    config:
      discovery:
        enabled: true
        service-id: config
      username: user
      password: password